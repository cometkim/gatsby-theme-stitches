export type PluginOptions = {
  /**
   * Path to stitches config module
   *
   * The modules should exports `css` instance.
   *
   * @default `stitches.config.js`,
   */
  configFilePath?: string,
};
